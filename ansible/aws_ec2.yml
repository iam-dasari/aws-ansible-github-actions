plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  "tag:Role":
    - web
  instance-state-name:
    - running

hostnames:
  - public-ip-address

compose:
  ansible_host: public_ip_address

keyed_groups:
  - prefix: role
    key: tags.Role

groups:
  web: "'web' == tags.Role"